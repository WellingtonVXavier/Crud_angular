{
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Angular Crud",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}